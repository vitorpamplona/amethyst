<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Login & Auth -->
    <!-- New Key Warning -->
    <!-- Key Backup (Desktop settings) -->
    <!-- Common Actions -->
    <!-- Errors -->
    <!-- Loading & Empty States -->
    <!-- Placeholder Screens -->
    <!-- Accessibility -->
    <!-- Relay history paging (shared feed markers + status card) -->
    <!-- Notes & Replies -->
    <!-- Static sites (NIP-5A) & napplets (NIP-5D) feed card -->
    <!-- Custom emoji suggestions (NIP-30) -->
    <!-- Nicknames (NIP-85 contact cards) -->
    <!-- NIP-CC geocaching (kinds 37516 / 7516) -->
    <string name="profile_image">Profile Image</string>
    <!-- Workout duration totals. %1$d is hours, %2$d is minutes; reorder and re-abbreviate
         freely — these are English abbreviations for hour/minute/second. -->
    <!-- Elapsed call time. %1$d is minutes and %2$d seconds in the short form; in the long
         form %1$d is hours, %2$d minutes and %3$d seconds. Separator and field order are the
         translator's to change. -->
    <!-- Marmot kind:1210 group system rows. Rendered as centered captions in the
         conversation, from the row's structured fields. %1$s is the member who
         committed the change, %2$s the member it concerns, %3$s the new name. -->
</resources>
